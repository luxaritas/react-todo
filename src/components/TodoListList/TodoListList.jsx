import React from 'react';
import {Panel} from 'components';

export default function TodoListList(props) {
  return (
    <Panel className={props.className} heading="Lists">
      
    </Panel>
  );
}
